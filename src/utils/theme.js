import {StyleSheet} from 'react-native';

const theme = StyleSheet.create({
    continerPrimary:{
        flex: 1,
        backgroundColor: '#0A0746',
        alignItems: 'center',
        justifyContent: 'center',
        padding:10
    }
})

export default theme